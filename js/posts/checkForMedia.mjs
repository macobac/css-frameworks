/* export default async function checkForMedia(e) {
    if (e.media) {
        const newImg = document.createElement("img");
        newImg.src = e.media;
        newImg.classList.add("card-img-top");
        return newImg;
    } else {
        console.log("theres no media");
    }
} */